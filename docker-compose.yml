version: '3'

services:
  app:
    build:
      context: .
      dockerfile: './Dockerfile'
    command: 'java -jar ./jar/recipes-api-0.0.1-all.jar'
    ports:
      - '10100:8080' # to machine port : from container port
    working_dir: '/app'
    volumes:
      - '.:/app'
